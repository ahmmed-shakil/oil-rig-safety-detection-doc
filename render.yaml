services:
  - type: web
    name: oil-rig-safety-system
    env: node
    region: oregon
    branch: main
  buildCommand: corepack enable && pnpm install && pnpm build
  startCommand: pnpm run start -- -p $PORT
    instanceType: starter
    envVars:
      - key: NODE_ENV
        value: production
